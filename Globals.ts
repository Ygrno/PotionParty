import { Card, ComponentCard, PotionCard, BonusCard } from "./classes/Card";

// Define the CardType enum
export enum CardType {
  SPIDERS,
  EYES,
  LIZARDS,
  MUSHROOMS,
  WORMS,
  JOKER,
  BONUS,
}
